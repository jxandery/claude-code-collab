---
description: Search through your prompt history
---

Search for prompts matching the query: {{query}}

```bash
~/.claude-sessions/scripts/claude-sessions search "{{query}}" --days {{days|30}} --limit {{limit|10}}
```

Show me the search results.
